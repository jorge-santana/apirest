import { ResponseDataFilterInterceptor } from './response-data-filter.interceptor';

describe('ResponseDataFilterInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseDataFilterInterceptor()).toBeDefined();
  });
});
